import { Box, Heading, Text } from '@chakra-ui/react';

const CourseList = () => {
  return (
    <Box>
      <Heading mb={4}>Available Courses</Heading>
      <Text>No courses available yet. Check back later!</Text>
    </Box>
  );
};

export default CourseList;